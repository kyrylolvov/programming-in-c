#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int num;
    printf("please enter a value");
    scanf("%d", &num);

    if (x = 0)
        printf("0");
    else
        printf("more 0");

        return 0
}
